<p id="notice"><%= notice %></p>

<p>
  <b>Title:</b>
  <%= @category.title %>
</p>

<p>
  <b>Description:</b>
  <%= @category.description %>
</p>


<%= link_to 'Edit', edit_shelf_category_path(@category) %> |
<%= link_to 'Back', shelf_category_path %>
<%= link_to 'Add products', store_product_cores_path(current_user.store_id) %>
