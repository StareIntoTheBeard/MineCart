<%= form_for(@shelf_page) do |f| %>
  <% if @shelf_page.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@shelf_page.errors.count, "error") %> prohibited this shelf_page from being saved:</h2>

      <ul>
      <% @shelf_page.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_area :content %>
  </div>
  <div class="field">
    <%= f.label :tags %><br />
    <%= f.text_field :tags %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
